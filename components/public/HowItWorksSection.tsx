/**
 * Re-export so existing imports (e.g. app/(marketing)/page.tsx) keep working.
 * Implementation lives in how-it-works/ for modularity.
 */
export { HowItWorksSection as default } from "./how-it-works";
