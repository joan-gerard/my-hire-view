/**
 * App-wide constants. Prefer keeping component-specific config in their own
 * constants modules (e.g. components/public/faq/constants.ts).
 */

/** Pixel width below which the layout is considered "mobile". Matches Tailwind's md breakpoint (768px). */
export const MOBILE_BREAKPOINT_PX = 768;
